{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install && npm react-scripts build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "~4.16.1",
    "heroku": "^7.39.1"
  }
}
